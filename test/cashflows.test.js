const assert = require('assert');
const Cashflows = require('../src/cashflows.js');

describe('Cashflows', () => {
	it('should be properly initialized', function() {
		assert.ok(true, 'should always succeed');
		// TBC
	});

    it('should be able to reach cashflows api', function() {
		assert.ok(true, 'should always succeed');
		// TBC
	});
});    
